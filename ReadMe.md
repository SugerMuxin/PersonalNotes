
Map

>原理：采用将地块分块的方式，分割为若干Chunks。通过管理Chunks，采用九宫格的方式动态加载和卸载 Chunks将内存的压力减少，目标在减少90%以上，视地图大小决定。

>限制：如果大地图能够缩小的时候可能面临一个短暂的瞬时加载压力，不过可以将地块上的对象按照优先级来分类，比如只需要加载和渲染基本地形，而不用加载物理阻挡和动态渲染对象来最小化瞬时压力，然后还可以使用LOD。如果再能缩小就使用缩略图的方式了。



